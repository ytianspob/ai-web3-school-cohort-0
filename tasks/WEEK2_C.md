GitHub Copilot
Identity-归属：Microsoft（GitHub），绑定在Github账户，可以在IDE里进行代码生成/理解
Capability-代码生成、补全、解释、建议
Input-结合上下文用户给出的信息，如文件、文字、指令等
Output-代码+解释文字
协作对象-IDE、Github、LLM、用户
失败点-上下文理解错误、AI幻觉


```mermaid
flowchart TD

A[这个Agent是谁] --> A1[数据分析Agent]
A --> A2[自部署或云平台]

B[由谁维护] --> B1[开发团队模型提供方 / 公司运维团队]

C[能做什么]
C --> C1[统计分析]
C --> C2[生成可视化图表]
C --> C3[生成业务报告]
C --> C4[提取关键洞察]

D[如何被调用] --> D1[API调用]
D --> D2[MCP工具调用]
D --> D3[Agent-to-Agent协作调用]

E[如何收费] --> E1[按数据量计费]
E --> E2[按调用次数计费]

F[如何被验证] --> F1[执行日志记录]
F --> F2[结果可复现校验]
F --> F3[链上或EAS证明]

G[失败如何处理] --> G1[输入错误 → 直接拒绝并返回错误信息]
G --> G2[执行失败 → 自动重试或降级模型]
G --> G3[结果不可靠 → 标记未验证]
G --> G4[超时 → 中断任务并返回部分结果]
